#include <stdio.h>
#include "lib/packet_fields.h"

enum return_codes {
    SUCCESS = 0,
    INVOCATION_ERROR = 1,
    FILE_ERROR = 2,
    MEMORY_ERROR = 3
};

int main(int argc, char *argv[])
{
	return (SUCCESS);
}
